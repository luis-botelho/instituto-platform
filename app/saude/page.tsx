import type { Metadata } from 'next'
import { PageHero } from '@/components/page-hero'
import { HEALTH_CHALLENGES } from '@/lib/observatorio-data'

export const metadata: Metadata = { title: 'Saúde em Mambucaba' }
export default function SaudePage() {
  return <main><PageHero eyebrow="Pesquisa territorial" title="O que os moradores disseram sobre a saúde em Mambucaba?" description="Uma leitura cidadã sobre acesso aos serviços, baseada em escuta comunitária e apresentada com seus limites."><a href="https://forms.gle/UpjrLZyTAWgFK9KZA" target="_blank" rel="noreferrer" className="inline-flex rounded-full bg-accent px-5 py-3 font-semibold text-accent-foreground">Participar da pesquisa de saúde</a></PageHero><section className="mx-auto max-w-6xl px-4 py-12"><div className="grid gap-5 md:grid-cols-3">{HEALTH_CHALLENGES.map((item) => <article key={item.title} className="rounded-2xl border bg-card p-6"><span className="font-serif text-4xl font-semibold text-primary">{item.value}%</span><h2 className="mt-3 font-serif text-xl font-semibold">{item.title}</h2><div className="mt-3 h-2 overflow-hidden rounded-full bg-secondary"><div className="h-full rounded-full bg-primary" style={{width:`${item.value}%`}} /></div><p className="mt-4 text-sm leading-relaxed text-muted-foreground">{item.text}</p></article>)}</div><div className="mt-10 grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><article className="rounded-2xl bg-primary p-7 text-primary-foreground"><p className="text-xs font-bold uppercase tracking-widest opacity-70">Metodologia</p><h2 className="mt-2 font-serif text-2xl font-semibold">Escuta exploratória</h2><p className="mt-3 leading-relaxed opacity-85">Questionário comunitário, participação voluntária e respostas autodeclaradas. Os percentuais apresentados reproduzem a leitura consolidada do protótipo anterior e devem ser confirmados antes de uso técnico ou institucional.</p></article><article className="rounded-2xl border bg-card p-7"><h2 className="font-serif text-2xl font-semibold">O que aprendemos</h2><div className="mt-5 grid gap-4 sm:grid-cols-2">{[['Acesso é o problema central','Espera, marcação e deslocamento formam uma experiência conectada.'],['Informação também é cuidado','Não saber onde, quando e como buscar atendimento amplia a barreira.'],['Os desafios se acumulam','Medicamentos, exames e consultas não podem ser analisados isoladamente.'],['Pesquisa é ponto de partida','A escuta orienta perguntas; dados oficiais e novas rodadas completam a análise.']].map(([t,d]) => <div key={t}><h3 className="font-semibold">{t}</h3><p className="mt-1 text-sm text-muted-foreground">{d}</p></div>)}</div></article></div></section></main>
}
